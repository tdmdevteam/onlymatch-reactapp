{
    "name": "my-react-app-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"php -S localhost:8000 -t backend/public\"",
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:backend": "php -S localhost:8000 -t backend/public"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "react-router-dom": "^7.8.0"
    }
}
